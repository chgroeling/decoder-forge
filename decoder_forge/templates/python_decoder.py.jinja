{%- macro match_pat(pat, first_child) -%}
    {% if first_child -%}
        if{{" "}}
    {%- else -%}
        elif{{" "}}
    {%- endif -%}

    (instr & {{-" 0x%x" % pat.fixedmask}}) == {{"0x%x" % pat.fixedbits-}}:  # {{pat}}
{%- endmacro -%}

{% macro gen_pat(pat, first_child, origin) -%}
    {{ match_pat(pat, first_child) }}
    {%- if origin == None %}
    {%- else %}
        # {{pat_repo[origin]["name"]}}
        # -> {{pat_repo[origin]["struct"]}}
{{ pat_repo[origin]["body"] | indent(8, first=True) }}
    {%- endif %}
{%- endmacro -%}

{% macro no_match() -%}
    return NoMatch(), {{min_instr_bytes}}  # no match
{%- endmacro -%}


# Auto-generated by decoder-forge. Do not edit.
#
# ARM pseudocode (the ``decode`` blocks) is transpiled to Python via arm-transpiller.
# The runtime those blocks call into is arm-transpiller's own ``armruntime``, embedded
# verbatim below, followed by the few decoder-forge additions layered on top of it.
from dataclasses import dataclass
from enum import IntEnum
from typing import ClassVar


# --- arm-transpiller armruntime (embedded verbatim) ------------------------------
{{ armruntime }}
# --- end arm-transpiller armruntime ----------------------------------------------


# --- decoder-forge additions -----------------------------------------------------
class DecoderState(IntEnum):
    """Bit-encoded decoder state flags set on each decoded instruction."""
    DECODER_NONE = 0
    DECODED_8BIT = 1
    DECODED_16BIT = 2
    DECODED_32BIT = 4


class Opcode(IntEnum):
    OP_NO_MATCH = -1
    OP_UNDEFINED = -2
    OP_UNPREDICTABLE = -3
    OP_SEE = -4
    {%- for struct in structs %}
    OP_{{ struct.name.upper() }} = {{ struct_id_map[struct.name] }}
    {%- endfor %}


@dataclass(frozen=True, eq=True)
class NoMatch:
    """Returned when no encoding matches the instruction word."""
    opcode: ClassVar[int] = Opcode.OP_NO_MATCH
    decoder_state: int = 0


@dataclass(frozen=True, eq=True)
class Undefined:
    """Pseudo-instruction returned when a decode flags the UNDEFINED side effect."""
    opcode: ClassVar[int] = Opcode.OP_UNDEFINED
    decoder_state: int


@dataclass(frozen=True, eq=True)
class Unpredictable:
    """Pseudo-instruction returned when a decode flags the UNPREDICTABLE side effect."""
    opcode: ClassVar[int] = Opcode.OP_UNPREDICTABLE
    decoder_state: int


@dataclass(frozen=True, eq=True)
class See:
    """Pseudo-instruction returned when a decode flags the SEE side effect.

    ``SEE <encoding>`` redirects decoding to another instruction. The runtime only
    records that the redirect fired (the target survives solely as a comment in the
    transpiled block).
    """
    opcode: ClassVar[int] = Opcode.OP_SEE
    decoder_state: int


def _apply_sideeffect(sideffect_flags, decoded, decoder_state):
    """Map a side effect flagged during decode onto a pseudo-instruction.

    ``SEE``, ``UNDEFINED`` and ``UNPREDICTABLE`` set bits on ``sideffect_flags``.
    Once the block has run, a flagged side effect replaces the decoded instruction with
    the matching pseudo instruction; otherwise the decoded instruction stands. ``SEE``
    wins (it redirects to another instruction, so the current decode does not apply),
    then ``UNDEFINED``, then ``UNPREDICTABLE``.
    """
    if sideffect_flags & SIDEFFECT_SEE:
        return See(decoder_state=decoder_state)
    if sideffect_flags & SIDEFFECT_UNDEFINED:
        return Undefined(decoder_state=decoder_state)
    if sideffect_flags & SIDEFFECT_UNPREDICTABLE:
        return Unpredictable(decoder_state=decoder_state)
    return decoded


def _bits(instr, lsb, width):
    return (instr >> lsb) & ((1 << width) - 1)
# --- end decoder-forge additions -------------------------------------------------

{{""}}
{%- for struct in structs %}
@dataclass(frozen=True, eq=True)
class {{struct.name}}:
    opcode: ClassVar[int] = Opcode.OP_{{ struct.name.upper() }}
    decoder_state: int
    {%- if struct.members|length > 0 %}
        {#- The annotation is how the value is represented in Python; the trailing
            comment names the ARM type it was inferred from, which also carries the
            value's bit width. No member has a default: an instruction is only ever
            constructed with all of its values present. #}
        {%- for member in struct.members %}
    {{ member.name }}: {{ member.annotation }}{% if member.arm_type and member.arm_type != member.annotation %}  # {{ member.arm_type }}{% endif %}
        {%- endfor %}
    {%- endif %}
    {{""}}
{%- endfor -%}

{{""}}
def get_decoder_eval_bytes():
    return {{needed_bytes_for_code_eval}};

{{""}}
def get_min_instr_bytes():
    return {{min_instr_bytes}};

{{""}}
def decode(instr: int, ctx: Context):
    # ``ctx`` is the name arm-transpiller emits for the runtime context argument.
    # Returns ``(result, n_bytes)``: the decoded instruction (or a NoMatch / See /
    # Undefined / Unpredictable pseudo-instruction) and the number of bytes it occupies,
    # so the caller can advance without a separate size pass.
    sideffect_flags = SIDEFFECT_NONE
{%- for pat, uid, depth, first_child, last_child in flat_decode_tree %}
    {%- set origin = uid_to_pat[uid] | default(None) %}
    {{ gen_pat(pat, first_child, origin) | indent(depth*4, first=True) }}
    {%- if loop.nextitem is defined %}
        {%- set backtrack = depth-loop.nextitem[2] %}
        {%- if backtrack > 0 %}
            {%- for bs in range(0, backtrack) %}
        {{no_match() | indent((depth-bs-1)*4, first=True)}}
            {%- endfor %}
        {%- endif %}
    {%- else %}
        {%- if depth>0 %}
        {{no_match() | indent((depth-1)*4, first=True)}}
        {%- endif %}
    {%- endif %}
{%- endfor %}
    {{no_match()}}